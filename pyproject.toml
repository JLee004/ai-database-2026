[project]
name = "ai-database-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.141.1",
    "pydantic==2.13.5",
    "psycopg[binary]==3.3.6",
    "uvicorn==0.53.0",
]

[project.scripts]
ai-database-2026 = "ai_database_2026:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
